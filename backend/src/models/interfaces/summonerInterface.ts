interface ISummoner {
    id: string,
    accountId: string,
    puuid: string,
    name: string,
    profileIconId: string,
    revisionDate: number,
    summonerLevel: number
}

interface INickSearch {
    nickname: string
}